#! /bin/bash
#@ job_name = lcs_parallel
#@ initialdir = ~/lcs_parallel/
#@ input = /dev/null
#@ output = $(job_name).out
#@ error = $(job_name).err
#@ class = short
#@ job_type = parallel
#@ blocking = unlimited
#@ total_tasks = 4
#@ queue

./lcs input10000.txt