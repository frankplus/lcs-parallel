#! /bin/bash
#@ job_name = lcs
#@ initialdir = ~/lcs_parallel/
#@ input = /dev/null
#@ output = $(job_name).out
#@ error = $(job_name).err
#@ class = medium
#@ job_type = parallel
#@ blocking = unlimited
#@ total_tasks = 8
#@ queue

./lcs input10000.txt